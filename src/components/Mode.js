import React from "react";

export class Mode extends React.Component{
    render(){
        return(
           <div className="mode">
               esto es un mode
            </div>
        )
    }
}

export default Mode;